# SLIDE 專案現況快照（供 Claude 工作排程分析用）

**產生日期**：2026-06-25  
**分析者**：Claude Code（自動掃描）  
**目的**：提供 Claude 完整的目前檔案狀態，作為規劃下一步工作排程的依據

---

## 1. 專案基本資訊

| 欄位 | 內容 |
|------|------|
| 專案名稱 | SLIDE（供應鏈物流智慧調度引擎） |
| 本地路徑 | `D:\anaconda_envs\SLIDE` |
| GitHub | `https://github.com/brightkhlin325-max/SLIDE` |
| 目前分支 | `main` |
| 最新 commit | `582fec1` — Merge PR #27 (feat/roi-simulator-and-diagnostics) |
| 與 GitHub 的 commit 差距 | **完全同步**（Ahead: 0 / Behind: 0） |
| Python 環境 | `D:\anaconda_envs\Fastapp` |

---

## 2. 最近 10 筆 Commit 歷程

| Hash | 說明 |
|------|------|
| `582fec1` | Merge PR #27 feat/roi-simulator-and-diagnostics |
| `8467f1d` | feat(roi): 最佳化ROI模擬器 + 模型診斷落地 (SLIDE 點1-4) |
| `7df0bd9` | Merge PR #26 feat/profit-prediction-data |
| `d781fe1` | feat(profit): 收益預測資料管線 + 原生類別整合 + 真資料重訓 (R²=0.81) |
| `0441f4c` | fix: make profit prediction page directly accessible |
| `b2a2ad7` | chore: add profit prediction demo artifacts |
| `266945b` | feat: add LightGBM profit prediction page |
| `31a4e27` | chore: clean up project structure |
| `8c4d501` | rebrand: EDIS → SLIDE (logo, magenta-purple theme) |
| `037ed12` | chore: refine UI wording and update requirements.txt |

---

## 3. 本地未提交變更（與 GitHub 的差異）

> **這些變更尚未 commit，也未 push 到 GitHub。**  
> GitHub 上目前看不到這些內容。

### 3-1. 已追蹤但已修改的檔案（10 個）

#### `app.py` (+273 / -0 行淨增，實際差分 +165 / -0 新增邏輯)

**變更摘要：**

1. **新增 `ASSISTANT_CACHE` 字典**  
   ```python
   ASSISTANT_CACHE: dict = {}  # (intent, params_json, pred_path_str) → response dict
   ```
   用途：AI 助理意圖路由的回應快取，避免重複 LLM 呼叫。

2. **`call_configured_llm()` 新增 `max_tokens` 參數**  
   - 預設值 400，支援 Gemini / Claude / OpenAI / Ollama 全部 provider。
   - 舊版固定 800（Claude），現在可依呼叫情境調整。

3. **全新 AI 助理意圖路由系統（新增約 165 行）**  
   - `route_assistant_intent(question)` — 解析使用者問題意圖，分類為：`order_lookup`、`high_risk_list`、`stats_summary`、`general_llm`
   - 正規表示式：`_ORD_RE`（ORD-XXXXXXXX 格式）、`_TOP_N_RE`（前 N / top N）
   - `handle_assistant_order_lookup()`、`handle_assistant_high_risk()` 等分流處理函數
   - 目的：讓 AI 助理更精準回應結構化查詢，減少不必要的 LLM token 消耗

#### `scripts/build_decision_dataset.py` (+15 / -7 行)

**變更摘要：LightGBM 模型相容性修正**

- 舊版：無條件套用 `category dtype`（只適用舊式含類別特徵的模型）
- 新版：先偵測模型是否有 categorical 設定，分兩條路：
  - **有 categorical 特徵**（舊式模型）→ 套用 `pd.Categorical`
  - **無 categorical 特徵**（新式模型）→ 全部 `.astype(float)` 直接使用整數碼

```python
booster_cat_features = set(booster.feature_name()) & set(cat_cols)
if booster_cat_features:
    # 舊式模型路徑
else:
    X = X.astype(float)  # 新式模型路徑
```

#### `setup_and_run.bat` (+9 行)

**變更摘要：雙擊啟動時 Conda 初始化修正**

新增 Step 0：偵測 `CONDA_SHLVL` 是否存在，若不存在（代表非 Conda Shell 啟動）則自動呼叫 `activate.bat`。支援兩個常見安裝路徑：
- `%USERPROFILE%\anaconda3\Scripts\activate.bat`
- `D:\anaconda3\Scripts\activate.bat`

**目的**：解決使用者直接雙擊 .bat 時 conda 環境無法啟動的問題。

#### `static/roi_simulator.js` (+10 / -2 行)

**變更摘要：KPI 格式化函數加強 null/NaN 防護**

| 函數 | 舊版（問題） | 新版（修復） |
|------|-----------|-----------|
| `_fmtMoney(v)` | 直接 `Math.round(v)`，NaN → `$NaN` | 先 `parseFloat`，null/NaN → `'—'` |
| `_fmtPct(v)` | 直接 `(v * 100).toFixed(1)`，NaN → `NaN%` | 先 `parseFloat`，null/NaN → `'—'` |

#### `static/components/roi_simulator.html` (+28 行)

**變更摘要：ROI KPI 卡片 CSS 修正**

新增 `#roiKpiRow .kpi-card` 的 Flex 直向排列規則，解決 KPI 卡片 label/value/foot 水平擠壓錯亂的問題：
- `flex-direction: column`
- `kpi-label`：11px muted 字
- `kpi-value`：22px 粗體
- `kpi-foot`：11px muted 字，`margin-top: 2px`

#### 資料與模型檔案（3 個 CSV + 1 個 JSON + 1 個 .txt）

| 檔案 | 變更規模 | 說明 |
|------|----------|------|
| `data/processed/profit_train_ready.csv` | +126,615 行 | 訓練集重新產出 |
| `data/processed/profit_test_ready.csv` | +27,135 行 | 測試集重新產出 |
| `data/processed/profit_val_ready.csv` | +27,135 行 | 驗證集重新產出 |
| `models/profit/serving_artifacts.json` | `created_at` 更新 | 2026-06-23 → 2026-06-24 09:47:28 UTC |
| `models/profit_lightgbm_model.txt` | 二進位模型更新 | 對應 CSV 資料重訓後的新模型 |

---

### 3-2. 未追蹤的新增檔案（不在 GitHub 上）

| 路徑 | 說明 |
|------|------|
| `BOBOBO/` | 子目錄，內有獨立 git 倉庫（`master` 分支），含 3 個 .md 檔 |
| `BOBOBO/EDIS_對話紀錄_20260617.md` | 舊版 EDIS 專案對話紀錄 |
| `BOBOBO/EDIS_對話紀錄_20260618.md` | 舊版 EDIS 專案對話紀錄 |
| `BOBOBO/EDIS_簡報與程式比對.md` | 簡報與程式碼比對分析 |
| `SLIDE_ROI模擬器_問題診斷與修復紀錄_2026-06-24.md` | ROI 模擬器問題診斷完整報告 |
| `fix_record.md` | 修復紀錄 |
| `fix_record.txt` | 修復紀錄（純文字版） |
| `pipe_err2.txt` | Pipeline 執行錯誤輸出 |
| `pipe_out2.txt` | Pipeline 執行標準輸出 |
| `pipeline_err.txt` | Pipeline 執行錯誤輸出（舊） |
| `pipeline_out.txt` | Pipeline 執行標準輸出（舊） |

---

## 4. 已知問題（來自 fix_record.md / 診斷報告）

### 主因：`decision_dataset.csv` 不存在

ROI 模擬器的所有 API（`/api/roi/summary`、`/api/roi/portfolio` 等）依賴此檔案：

```
app.py → _load_decision_df()
       → data/processed/decision_dataset.csv  ← 缺少！
```

此檔案需由 `scripts/build_decision_dataset.py` 產生，而該腳本需要原始資料集 `DataCoSupplyChainDataset.csv` 放在 `data/raw/`。但 `data/raw/` 目錄只有 `.gitkeep`（原始資料不入版控）。

**症狀：**
- ROI 模擬器所有 KPI 顯示 `$非數值` / `--` / `NaN%`
- 真價值—風險散點圖空白
- 在險利潤名單顯示「無資料（0 筆）」

**現況：** `build_decision_dataset.py` 已完成相容性修正（見 3-1），但原始資料仍需手動補入。

---

## 5. 工作排程建議事項（給 Claude 分析用）

以下是根據現況整理出的**待處理工作項目**，請 Claude 協助排優先順序與估時：

### 高優先級（功能阻斷）

- [ ] **補入原始資料** `DataCoSupplyChainDataset.csv` → `data/raw/`，並重跑 `build_decision_dataset.py` 產生 `decision_dataset.csv`
- [ ] **驗證 ROI 模擬器**：資料補入後，確認所有 KPI、散點圖、在險名單正常顯示

### 中優先級（本地變更需整理與 push）

- [ ] **將目前 10 個已修改檔案整理成 commit**
  - 建議拆分：
    1. `feat(assistant): AI 助理意圖路由 + LLM max_tokens 可調`（app.py）
    2. `fix(profit): LightGBM 新舊模型相容性 + 資料重訓`（CSV + model + build script）
    3. `fix(roi): KPI null 防護 + 卡片排版 CSS`（roi_simulator.js + html）
    4. `fix(bat): 雙擊啟動 Conda 初始化`（setup_and_run.bat）
- [ ] **決定 BOBOBO/ 子目錄的歸屬**：是否加入 `.gitignore` 或獨立管理

### 低優先級（清理）

- [ ] **刪除或歸檔 pipeline log 檔**：`pipe_*.txt`、`pipeline_*.txt`（建議加入 `.gitignore`）
- [ ] **整理診斷紀錄**：`SLIDE_ROI模擬器_問題診斷與修復紀錄_2026-06-24.md`、`fix_record.*` 是否保留或移入 `docs/`

---

## 6. 其他子目錄現況

| 目錄 | Git 倉庫 | 最後修改 | 說明 |
|------|----------|----------|------|
| `D:\anaconda_envs\AI` | 否 | 2026-04-30 | 無 git，無 GitHub 連結 |
| `D:\anaconda_envs\Fastapp` | 否 | 2026-06-10 | Python 執行環境目錄 |
| `D:\anaconda_envs\SLIDE` | **是** | 2026-06-24 | 主專案，詳見上方 |

---

*此檔案由 Claude Code 自動產生，反映 2026-06-25 上午的本地狀態。*
